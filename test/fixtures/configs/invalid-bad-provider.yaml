# Invalid configuration - unsupported cloud provider
global:
  cloud_provider: gcp
  timeout: 3600
  log_level: info

environments:
  dev:
    network_cidr: 10.0.0.0/16
    db_instance_class: db.t3.micro
    db_allocated_storage: 20
    db_multi_az: false
    k8s_node_count: 2
